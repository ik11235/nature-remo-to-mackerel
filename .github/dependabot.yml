version: 2
updates:
  # npmの依存パッケージを持たないため、監視対象はGitHub Actionsのみ
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      # actionsのバージョン更新は個別にPRを作らずまとめる
      github-actions:
        patterns:
          - '*'
